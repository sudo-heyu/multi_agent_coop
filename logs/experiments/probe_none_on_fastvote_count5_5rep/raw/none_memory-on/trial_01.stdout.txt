[run] runtime=ppio-stream model=qwen/qwen3.6-35b-a3b scene=sr server=http://127.0.0.1:5001 max_steps=8 tool_profile=none memory=on acceptance=qos
执行推送端点：{'ap1': 'http://localhost:5003', 'ap2': 'http://localhost:5003', 'ap3': 'http://localhost:5003'}
[State] 复用常驻 state server http://127.0.0.1:5001
[Runtime] 使用 PPIO stream runtime（不依赖 OpenClaw gateway）
[State] ns3 模式：等待 ns-3 bridge 上报（source=ns3）
[Run] session_id=d5a37f9f event_db=logs/experiments/probe_none_on_fastvote_count5_5rep/db/none_memory-on.sqlite3
[Trace] 连续遥测落盘：logs/state/telemetry_trace_20260710_042219_d5a37f9f.jsonl

[1m[96mAP1[0m:
我是 AP1。以下是本机实测状态播报：
MAC/EDCA 参数：发射功率（TX Power）为 10 dBm；退避窗口 CWmin/CWmax 为 31/63，AIFSN 为 7；当前业务标识为 best_effort，流量优先级为 medium。
感知指标：己方关联 STA（sta_ap1_user）的接收信号强度（STA RSSI）为 -45.71 dBm；本机扫描到的邻居 AP 信号强度为：ap2 约 -80.99 dBm，ap3 约 -71.96 dBm。噪声底噪、信道利用率及 TX Retries Ratio 在当前状态快照中未提供具体数值。
业务质量：用户侧吞吐量（iperf/user）为 0 Mbps，端到端延迟 0 ms，丢包率 0%。
SLA/QoE 反馈：关联 STA 的 SLA 状态显示为 violated，核心违规项为吞吐量（0.0 Mbps）未达到协议要求的最低阈值（≥0.5 Mbps）。

状态简述：本机 STA 物理层连接质量良好，但业务层出现零吞吐异常；邻居干扰整体偏弱（ap3 信号约 -72 dBm），信道竞争参数当前处于较保守状态。本轮仅做状态同步，暂不提出调整建议。

[1m[92mAP2[0m:
我是 AP2。以下是本机实测状态播报：
MAC/EDCA 参数：发射功率（TX Power）为 10 dBm；基础服务集（BE）退避窗口 CWmin/CWmax 为 7/15，AIFSN 为 2；视频服务集（VI）退避窗口 CWmin/CWmax 为 7/15，AIFSN 为 2。
感知指标：己方关联 STA（sta_ap2_user）的接收信号强度（STA RSSI）为 -45.71 dBm；本机扫描到的邻居 AP 信号强度为：ap1 约 -81.0 dBm，ap3 约 -72.0 dBm。
业务质量：用户侧吞吐量（iperf/user）为 8.05 Mbps，端到端延迟 13.81 ms，抖动 2.04 ms，丢包率 0%。
SLA/QoE 反馈：关联 STA 的 SLA 状态显示为 satisfied，无违规项，QoE 表现稳定。

状态简述：本机物理层连接质量良好，邻居干扰整体偏弱，业务吞吐与延迟均符合预期，网络状态平稳。

[1m[93mAP3[0m:
我是 AP3。以下是本机实测状态播报：
MAC/EDCA 参数：发射功率（TX Power）为 10 dBm；基础服务集（BE）退避窗口 CWmin/CWmax 为 15/31，AIFSN 为 3；视频服务集（VI）退避窗口 CWmin/CWmax 为 15/31，AIFSN 为 3。
感知指标：己方关联 STA（sta_ap3_user）的接收信号强度（STA RSSI）为 -45.71 dBm；本机扫描到的邻居 AP 信号强度为：ap1 约 -72.0 dBm，ap2 约 -72.0 dBm。噪声底噪、信道利用率及 TX Retries Ratio 在当前状态快照中未提供具体数值。
业务质量：用户侧吞吐量（iperf/user）为 8.29 Mbps，端到端延迟 22.98 ms，抖动 2.10 ms，丢包率 0%。
SLA/QoE 反馈：关联 STA 的 SLA 状态显示为 satisfied，无违规项，QoE 表现稳定。

状态简述：本机物理层连接质量良好，邻居干扰整体偏弱（约 -72 dBm），业务吞吐与延迟均符合预期，网络状态平稳。

[1m[96mAP1[0m:
【记忆锚定提案】召回到高质量 improved 历史案例，本轮直接复用该已验证动作以减少重复试错。
quality=1.0, score=0.329489
```json
{
  "ap1": {
    "CWmin": 15,
    "CWmax": 31,
    "AIFSN": 3,
    "VI_CWmin": 7,
    "VI_CWmax": 15,
    "VI_AIFSN": 2
  },
  "ap2": {
    "CWmin": 15,
    "CWmax": 31,
    "AIFSN": 3,
    "VI_CWmin": 7,
    "VI_CWmax": 15,
    "VI_AIFSN": 2
  },
  "ap3": {
    "CWmin": 15,
    "CWmax": 31,
    "AIFSN": 3,
    "VI_CWmin": 7,
    "VI_CWmax": 15,
    "VI_AIFSN": 2
  }
}
```

[1m[92mAP2[0m:
{"agreed": true, "reason": "当前提案与高质量 improved 记忆案例完全一致，历史 quality=1.0, score=0.329489；本轮优先复用已验证动作以减少重复试错。"}

[1m[93mAP3[0m:
{"agreed": true, "reason": "当前提案与高质量 improved 记忆案例完全一致，历史 quality=1.0, score=0.329489；本轮优先复用已验证动作以减少重复试错。"}

[1m[96mAP1[0m:
{"ap1": {"CWmin": 15, "CWmax": 31, "AIFSN": 3, "VI_CWmin": 7, "VI_CWmax": 15, "VI_AIFSN": 2}, "ap2": {"CWmin": 15, "CWmax": 31, "AIFSN": 3, "VI_CWmin": 7, "VI_CWmax": 15, "VI_AIFSN": 2}, "ap3": {"CWmin": 15, "CWmax": 31, "AIFSN": 3, "VI_CWmin": 7, "VI_CWmax": 15, "VI_AIFSN": 2}}
协商结束[90m------------------------------------------------------------------------[0m
[1m[34m[结果][0m outcome=[1m[91mmax_retries_exceeded[0m turns=9 用时 44s
[1m[34m[策略][0m co_edca
[1m[34m[最终决策][0m None
[1m[35mValidator[0m [1m[91m未通过[0m — 验证失败（策略=co_edca，1 项错误）：决策后引入新的 STA SLA 违规: ['sta_ap3_user']
